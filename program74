//To print number of words in a string

#include<stdio.h>
int main()
{
	char sen[100];
	scanf("%[^\n]s",sen);
	int count=0;
	int i;
	for(i=0;sen[i]!='\0';i++)
	{
		if(sen[i]==' ')
		{
			count++;
		}
	}
	printf("%d",count+1);
}
